/*import React from 'react';
import { Box, Button, Typography } from '@mui/material';
import '../Pages/PageBienvenue.css';

function PageBienvenue() {
  const history = useHistory();

  const handleClique = () => {
    // Rediriger vers la page du popup lorsque le bouton est cliqué
    history.push('/popup');
  };

  return (
    <Box className="pageBienvenueContainer"> 
      <Typography variant="h3" gutterBottom>
        Bienvenue !
      </Typography>
      <Typography variant="h5" gutterBottom>
        Vous souhaitez :
      </Typography>
      <Button
        variant="contained"
        size="large"
        onClick={handleClique}
      >
        Suivre le questionnaire
      </Button>
    </Box>
  );
}

export default PageBienvenue;*/

import React from 'react'

function PageBienvenue() {
  return (
    <div>PageBienvenue</div>
  )
}

export default PageBienvenue